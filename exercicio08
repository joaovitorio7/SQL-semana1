a)

SELECT *
FROM CAMPEOES
ORDER BY ano;

b)

SELECT sigla_time
FROM CAMPEOES
WHERE ano = (SELECT MAX(ano) FROM CAMPEOES);

c)

SELECT sigla_time, COUNT()
FROM CAMPEOES
GROUP BY sigla_time
ORDER BY COUNT() DESC;